---
repo: krishanraja/fractionl-pulse
product: Pulse by Fractionl
as_of: 2026-09-24
head: 5ca0c60
lifecycle: live
production_url: https://pulse.fractionl.ai
state_doc: docs/DOCUMENTATION_GOVERNANCE.md
history_log: docs/history/LOG.md
truth_files: [public/product-truth.json, public/llms.txt, public/.well-known/ai-plugin.json]
authority_order: [production readback, runtime code and applied database state, docs/CORPORATE_STRATEGY.md, public/product-truth.json, task-specific guides under docs/]
steward: https://github.com/krishanraja/control-center/blob/main/docs/steward/RUNBOOK.md
never_publish: [the product name and its domain, the index name and abbreviation, the current score or label, any named data source or provider, pricing hypotheses and validation targets, Supabase project ids, credential and secret names, alert mailbox addresses, per-provider spend figures]
---
# Pulse by Fractionl: where it is right now

## What it is

Pulse is the independent market instrument for fractional leadership: a free, public 0 to 100 Fractional Working Index (FWI) that shows whether demand for fractional C-suite leaders is expanding, which roles are moving, and how strong the evidence is. Twenty tracked inputs feed three pillars (Demand 50%, Supply 20%, Culture 30%); the score is recalculated after each successful daily ingest and read through a weekly brief and seven-day role windows. It is a React and Supabase build on Vercel: edge functions collect and normalise the inputs, a public REST API and a hosted MCP server serve the reading to people and agents, and per-source health, completeness and provenance are published next to the number. Published by Fractionl, not an official index. The commercial hypothesis, a private cross-partner benchmark for specialist fractional-talent firms, is not live (`docs/CORPORATE_STRATEGY.md`).

## Who it is for and why it matters for Mindmake

Pulse is not a Mindmake product and Mindmake does not sell it. Its own audiences and buyer are in `docs/CORPORATE_STRATEGY.md`. For Mindmake it is proof. The room_face buyer (`control-center/docs/ICP.md`: a senior leader at a PE or VC backed media, adtech, publishing or data business, quietly behind on what is coming) runs or funds a data business. An independent market index with 20 inputs, a daily pipeline, published methodology, per-source health, a public API and an MCP server is the kind of asset that business would scope as a quarter's work for a team. One person built it and runs it with AI, in public, with the failures left in.

Angles a writer can use (the registry anonymises this repo; say "a side build", never the name):

- **The honest instrument.** Every reading carries its completeness and per-source health. Thirty-eight days of history carry an explicit "simulated estimate" label rather than being passed off as measured, and a committed baseline makes any change to that count an incident (`docs/WEEKLY_PIPELINE_AUDIT.md`, `docs/audit-baseline.json`).
- **Read the body, not the status code.** Two collectors were dead for weeks while every run reported success: one because a vendor retired a build tag the run URL pinned, one because an in-function auth check rejected the pipeline's own calls. The fix commit of 29 August 2026 names both and how long each had been dead. The weekly audit exists because "a pipeline that reports success while writing nothing has failed."
- **Provider concentration, measured in index weight.** One exhausted vendor account took out four inputs on 4 August 2026. The cutover to a second provider kept an independent backstop for the most exposed proxy, and the audit script groups failures by the credential that gates them so five dead sources report as one vendor account (`docs/DATA_SOURCES_ROADMAP.md`).
- **Discipline about claims.** The repo's own rules forbid calling the index first, only, real-time or predictive, and forbid turning a migration or a feature flag into a live-product claim (`docs/DOCUMENTATION_GOVERNANCE.md`). That is the standard the buyer's own data team should be held to.

Objection it answers: "One person with AI can prototype, but cannot run a data product." Here is one that has accumulated daily observations since June 2026, with its audit trail in the open.

## Where it is right now (as of 2026-09-24)

- **Lifecycle: live**, in the governance doc's own vocabulary: a user can use the public instrument in production now. **Production was read directly on 2026-09-24** (`data_source_health`, `pipeline_runs`, `signals`, `fwi_scores`, `supabase_migrations.schema_migrations`): the FWI reads **50.30 for 2026-09-24 at 0.93 completeness**, and the FWI pipeline carries **20 health rows, 18 healthy**, the two not healthy being `hn` and `gofractional`. That readback settles the "20 in code, 21 in production" question left open on 2026-09-21: production reads 20. `public/product-truth.json` `production_snapshot` and `docs/DATA_SOURCES_ROADMAP.md` section 1 are still stamped 11 August 2026 and remain stale; this file, not those, carries the current reading.
- **Live (readback 2026-08-11, schedule readback 2026-09-21):** the public FWI with demand, supply and culture components; twelve months of mixed-frequency history; six US-scoped role-demand pages; the weekly brief; Ask the Index; public REST endpoints; four hosted MCP tools; discovery files at `/product-truth.json`, `/llms.txt` and `/.well-known/ai-plugin.json`; optional accounts and free operational API keys.
- **Schedule, deployed and read back 2026-09-21:** `ingest-signals` now collects once per calendar day and answers a later caller with `{"skipped": true}` rather than paying every provider twice; the Monday-only Vercel weekly cron, a pure duplicate, is removed. The daily sequence is Supabase `pg_cron` job prep 05:00 UTC, Vercel primary ingest 06:00, Supabase insights refresh 06:20, prerender rebuild 06:40, Supabase backstop ingest 08:00 (no-ops unless the primary failed), second rebuild 08:40 (`docs/TECHNICAL_SPEC.md` section 2, migrations `017`-`018`). Before this fix the pipeline ran two to four times a day, most visibly three times on Mondays, paying every provider for rows the upsert then deduplicated.
- **Fixed in code and deployed, 2026-09-21:** source health is now attributed from rows actually persisted, not from a collector's success flag; a source that reports success but has every reading dropped by the empty-supply or anomaly guard is recorded failed with `last_success` preserved rather than stamped healthy. Absence in the trends parser (a null reading or a `missing_data` period) is now excluded from the average instead of counted as a measured zero, which had been landing on exactly 0 on about half the days for one source. The alert sending domain's DNS is verified (the 2026-08-07 "unverified" note was six weeks stale); every alert now records which address actually delivered rather than assuming the primary path works (`docs/DATA_SOURCES_ROADMAP.md` section 3).
- **Retired, and confirmed fully live on 2026-09-24:** `serpapi_supply_trends` (four Google Trends supply-intent terms that sat at the reporting floor and normalised to a constant 5-6, pulling the supply pillar down 5.42 points on roughly half of all days). Its weight redistributed pro rata within the supply pillar; the claim surface across 22 files now says 20 tracked inputs. The 2026-09-21 note that this had not landed was wrong: the 2026-09-24 readback shows migration `019` applied (`20260921200333`), no `serpapi_supply_trends` row in `data_source_health`, and no `signals` row from it since 2026-09-18. Both halves shipped on 2026-09-21: the migration at 20:03:33 UTC, the function deploy at 20:09:43 UTC, six minutes apart. That is the reverse of the order `019`'s own header prescribes; it was harmless only because no ingest ran in the gap (collection is daily at 06:00). The ordering rule stands for the next retirement.
- **Added, collected, zero weight:** `sec_exec_transitions`, a first search-independent input (SEC 8-K Item 5.02 filings for "interim chief financial officer" / "interim chief executive officer"). Probationary: excluded from the composite and from the published input count until validated (`docs/TECHNICAL_SPEC.md` section 4). Confirmed collecting in the 2026-09-24 readback, with `signals` rows through today.
- **Held-out validation anchor, deployed 2026-09-24:** the JOLTS professional & business services job-openings series (`JTS540099000000000JOL`), the anchor `docs/DATA_QUALITY_STRATEGY.md` §1 names as the closest official measure of the demand the FWI claims to track. Collected under its own category `job_openings_pbs` alongside the existing total nonfarm series rather than replacing it, because the two sit at different levels (trailing-12 means 1,237 vs 7,106 thousand openings) and reusing the `job_openings` category would write a step change into the column `scripts/validate-anchor.mjs` correlates on. Both are `signal_type: 'context'`, excluded from the composite, so weights, methodology and the published 20-tracked-inputs count are unchanged: `TRACKED_INPUTS` enumerates sources and `bls` is one entry. **Its first row is not yet collected:** the deploy landed after today's 06:00 ingest, and the once-per-day guard correctly skipped a manual invocation, so the first `job_openings_pbs` row is expected from the 2026-09-25 06:00 UTC run. Verify it landed before treating this as done.
- **Measured, not yet acted on:** the evidence base is now scored, not just counted (`npm run quality`, `docs/DATA_QUALITY_STRATEGY.md`). Effective independent inputs measured at 10.7 of 20 tracked (53.5%), against a pre-registered hypothesis of roughly 5 that the measurement itself disproved. A 500-draw bootstrap over which sources arrived puts the published one-decimal score inside a 90% span of about 16.7 points on the latest reading; whether to publish a whole number with a stated band instead of a false decimal of precision is **waiting on Krish**.
- **Validation offer, not revenue:** Founding Benchmark Partner, application only (`src/pages/Pricing.tsx` shows it and the enterprise tier; the membership tier is strategy-defined and not displayed). Benchmark Membership and Enterprise are **Conditional** on partner-data gates that do not exist yet. Partner-data ingestion, the cohort dashboard, suppression rules and partner billing are **Planned** (`public/product-truth.json` `not_live`).
- **Legacy dormant:** consumer subscription, Stripe checkout and waitlist infrastructure remain in the tree and are not offers.

## What changed recently

- 2026-09-24 **§1 of the data-quality strategy executed for the first time, and the answer was "too early to tell"** (`2b0450d`, `scripts/validate-anchor.mjs`, `npm run validate`). Why: the programme adopted on 2026-09-21 had never been run, so the FWI had still never been checked against an external measure of what it claims to track. The script correlates the FWI against the three context inputs already excluded from the composite (`bls`, `fred`, `census_acs`), collapsed to one row per official release rather than one per day, because the collector snaps the latest published value daily and a naive daily join would inflate n by 20-30x, the exact pseudo-replication §1 warns about. Result: BLS JOLTS and FRED ICSA have 5 independent releases each since the daily era began, so every CI but one spans nearly the full [-1, 1]. Census ACS has had one distinct value in 116 days and cannot anchor anything. The one CI excluding zero (FRED ICSA against the culture pillar, r = -0.90) is explicitly **not** claimed: eight pillar × anchor pairs were tested at n = 5, and one such result is the expected outcome of that many comparisons. Nothing is published from this; the finding is that the pipeline is too young to validate, stated precisely rather than approximately.
- 2026-09-24 **The JOLTS professional & business services anchor wired and deployed** (`13f6e60`, `ingest-signals` v65). Why: §1 named that series as the better anchor, but only total nonfarm was collected, so the clock on the anchor that matters had never started. The series ID was verified before wiring, not assumed: `JTS540099000000000JOL` is confirmed against the BLS series directory (`download.bls.gov/pub/time.series/jt/jt.series` and `jt.industry`) as seasonally adjusted, job-openings level, industry `540099` = "Professional and business services", covering 2000-M12 to 2026-M07. The spelling recorded as unverified on 2026-09-24 turned out to be correct; the plausible-looking `540000` was the wrong answer. Added alongside total nonfarm, not in place of it: same BLS request, no extra call or quota. Baseline 1200 is the trailing-12 mean (1,237) rounded on the same convention as total nonfarm's 7000 against its 7,106.
- 2026-09-24 **Production read back directly, and it corrected this file** (no commit; readback recorded above). Why: `NOW.md` had claimed the `serpapi_supply_trends` retirement was "not yet deployed" and that production still showed 21 sources. Both were false. Migration `019` was applied 2026-09-21 at 20:03:33 UTC and the function deployed at 20:09:43; production reads 20 FWI health rows, 18 healthy. The claim had gone stale because the 2026-09-21 verification note was written from the commit history rather than from the database, which is the failure mode the authority order exists to prevent: live state beats documentation.
- 2026-09-21 **Five pipeline integrity defects fixed, from a same-day data-feed audit** (`183393a`, `3d05a30`). Why: each was a status trusted over the thing it describes. Source health had been stamped `healthy` with `last_success = now` whenever a collector reported success, even when every one of its readings was then dropped by the empty-supply or anomaly guard; `hn` and `serpapi_supply_trends` both carried a same-day `last_success` with no signal row in days. The trends parser had coerced a missing reading to 0, landing one source's four-point tail average on exactly 0 on 57 of 113 days, each then dropped as an "empty" supply signal. Vercel's daily cron, Vercel's Monday cron and two Supabase `pg_cron` backstops all fired at 06:00, so `ingest-signals` ran twice a day and three times on Mondays, paying every provider for rows the upsert then deduplicated; it now collects once per calendar day. Of "seven stale health rows" flagged, six were the content radar's own sources being checked against the FWI's weight table; only `bls_oews` (untouched since 2026-05-31) was genuinely dead and is deleted.
- 2026-09-21 **Schedule reconciled against production after the deploy** (`85a04d5`). Why: `docs/TECHNICAL_SPEC.md` section 2 said pg_cron did not show the daily-ingest, daily-insights or daily-redeploy jobs, a note dated 11 August that was wrong when written or overtaken since; that gap is how the duplicate collection above ran unnoticed. Verified after deploy: a second same-day call to `ingest-signals` returned `{"skipped": true, "reason": "already_collected_today"}` and spent nothing at any provider.
- 2026-09-21 **A 10/10 standard added to the weekly audit, above "healthy"** (`3ea9363`). Why: the audit answered whether the pipeline was healthy and never whether it was good, so a green week was never an improvement week. Section 8 scores seven dimensions against what a 10 would look like and requires one named, ranked improvement per week; "nothing to improve" means the review was not done. Deliberately not scripted: a 10/10 standard expressed as an assertion becomes a threshold to pass, not a standard.
- 2026-09-21 **`serpapi_supply_trends` retired, Krish's decision, weight redistributed within supply** (`a6815c7`). Why: not a broken collector, which is why nothing caught it for months. Its four Google Trends supply-intent terms sat at the reporting floor; on the 56 of 113 days since 2026-06-01 it contributed, the four-term average normalised to a constant 5-6 and pulled the supply pillar from 65.13 to 59.71, a headline effect of -1.08 on roughly half of all days. History is not restated. The claim surface moved from 21 to 20 tracked inputs across 22 files. Not deployed: the production verification later the same day (`c319148`) still showed 21 healthy sources.
- 2026-09-21 **Data-quality and evolution strategy adopted** (`1aff5b2`, `docs/DATA_QUALITY_STRATEGY.md`). Why: "how many inputs" is a count, and a count cannot see the thing that made `serpapi_supply_trends` worth retiring. The programme: validate the index against BLS/FRED/Census as a held-out set; publish the effective number of independent inputs; bootstrap an error band; judge new inputs on marginal information, not on being free; retire or re-specify any input a weekly ratchet finds redundant or immaterial. Runs as a separate Thursday session from the Monday integrity audit so a concrete failing collector never crowds out the correlation work.
- 2026-09-21 **The 10x programme enacted: the evidence base measured, a false-precision finding, a first search-independent input** (`c319148`). `npm run quality` measured effective independent inputs at 10.7 of 20 tracked (53.5%), against a pre-registered hypothesis of about 5 that the measurement disproved and kept rather than edited out. A 500-draw bootstrap over which sources arrived put the latest published one-decimal score inside a 90% span of 16.7 points, a finding left for Krish rather than acted on unilaterally. `sec_exec_transitions` (SEC 8-K Item 5.02 filings, two terms only after five extensions were rejected on the spot for sitting at the reporting floor) shipped probationary: collected, zero weight, excluded from the composite and the published count. Ruling (Krish, 2026-09-21): a source that reports success but persists nothing is AMBER, not RED; RED stays reserved for the index being wrong or absent.
- 2026-09-08 **`AGENTS.md` added, carrying the shared canon block rendered from `krishanraja/ai-harness`** (`fc91783`, synced twice since to release v2026.09.08.2 at `7c124e2`; sha `0cb0b3e5e848`). Why: every AGENTS.md in the fleet now reads off one canon; before this commit the canon existed but this repository referenced it zero times. The block is marker delimited and carries the sha256 of its own body, so drift is arithmetic rather than judgement; the docs steward never edits between the markers. Codex reads `AGENTS.md` natively, and it states that this repository's own rules (`docs/AGENT_BRIEFING.md`, `docs/DOCUMENTATION_GOVERNANCE.md`) outrank the canon on structure, naming, voice, stamps and archive location.
- 2026-09-08 **Freshness claim in the `AGENTS.md` header corrected** (`41bea6d`, outside the canon markers). Why: the header said `NOW.md` is reconciled against the code on every push to `main` and nightly. The push half was never true: `claude-code-action` refuses the push event, so the steward had failed on every push since it shipped (the same defect the steward runbook records: shipped 2026-09-07 with `push` in the trigger list and no mode split, failing every push for a day). The header now says the push run validates only and the nightly run does the reconciling.
- 2026-08-29 **Two production fixes restored to `main`** (`a123e81`, `7df98a5`). Why: the repo had drifted from what was running. The GoFractional collector had failed on every run since 2026-07-03 because the vendor retired the `latest` build tag its run URL pinned; the fix drops the pin and adds the proxy configuration the newer build requires. Insights generation had returned 403 on every daily cron call since 2026-08-10 because an in-function check compared the bearer token to the service-role key; the check was removed and the gateway's JWT verification is the auth. The merge conflict was `origin/main` reintroducing the broken URL, "resolved in favour of the fix." No document moved at the time; `docs/TECHNICAL_SPEC.md` and `docs/DATA_SOURCES_ROADMAP.md` were reconciled on 2026-09-07.

## What is next and what is waiting on Krish

- Done 2026-09-24, was the previous "Next": the production readback happened and the source count reads 20. The `serpapi_supply_trends` retirement and migration `019` were already deployed on 2026-09-21; nothing was outstanding.
- Next: confirm the first `job_openings_pbs` row lands in the 2026-09-25 06:00 UTC ingest (`select * from signals where source='bls' and category='job_openings_pbs' order by date desc limit 3`). The deploy is verified but the collector path for this series has not yet executed once. If it is absent, the series ID or the BLS response shape is the first place to look. Note BLS's keyless API quota is shared per IP and was already exhausted from this container on 2026-09-24, which is why the ID was verified against the flat-file directory instead.
- Next: `hn` and `gofractional` are the two FWI sources not healthy in the 2026-09-24 readback (`hn` last success 2026-09-21, last signal row 2026-09-16; `gofractional` last success 2026-09-22). `hn` has now been failing long enough that the 2026-09-21 integrity work, which was meant to make exactly this visible, has done its job and the failure needs diagnosing rather than watching.
- Next: `public/product-truth.json` `production_snapshot` and `docs/DATA_SOURCES_ROADMAP.md` section 1 are stamped 2026-08-11 and are now six weeks stale, through two rounds of pipeline change. They contradict the 2026-09-24 readback above and should be refreshed from it.
- Waiting on Krish: whether to publish the FWI as a whole number with a stated error band instead of one decimal place; the bootstrap in `docs/DATA_QUALITY_STRATEGY.md` puts the latest reading's 90% span at 16.7 points, two orders of magnitude coarser than a tenth of a point.
- Waiting on Krish: whether the Apify account behind the GoFractional collector needs attention; a rejected proxied run is now retried once without a proxy, which keeps the source delivering but does not address the underlying allowance. A budget note in the commit history puts remaining headroom at roughly $10 to 1 October 2026 against the one Apify caller left.
- Waiting on Krish: the commercial validation gate of 31 October 2026 (25 qualified conversations, ten letters of intent, five paid pilots) in `docs/CORPORATE_STRATEGY.md` section 8. No progress figures exist in the repo; the steward records none.
- Waiting on Krish: `docs/FLEET_WIRING.md` and the governance ownership map say "Mindmaker OS". The OS was renamed Mindmake on 2026-08-29 (control-center). Renaming is a naming decision, not drift the steward resolves.

## Read next

The authority order is the truth hierarchy in `docs/DOCUMENTATION_GOVERNANCE.md`: production readback, then runtime code and applied database state, then the documents below.

0. `AGENTS.md`: the entry file Codex reads natively; Claude Code and Cursor are routed to it by their own rules. Points here first, then to this repository's own rules. Carries the marker-delimited canon block rendered from `krishanraja/ai-harness`; the docs steward reads it and never writes inside the markers.
1. `docs/DOCUMENTATION_GOVERNANCE.md`: which statement wins, the status vocabulary (Live, Validation offer, Conditional, Legacy dormant, Planned), the reconciliation checklist.
2. `docs/CORPORATE_STRATEGY.md`: the canonical commercial strategy, ICP, pricing hypotheses and evidence matrix. Commercial claims come from here and are Krish's.
3. `public/product-truth.json`, `public/llms.txt`, `public/.well-known/ai-plugin.json`: the machine contract agents fetch before quoting anything; they move with the prose.
4. `README.md`: scope, live surfaces, API, architecture, setup, document map.
5. `docs/TECHNICAL_SPEC.md`: edge functions, schedule, schema, sources, composite, API. Reconciled against `main` at `c319148`; section 2's schedule carries its own 2026-09-21 production readback.
6. `docs/DATA_SOURCES_ROADMAP.md`: the 20 inputs, confidence weights, incidents, cost model, roadmap. Code side reconciled at `c319148`; database side last read 2026-08-11, so it does not yet reflect the 2026-09-21 integrity fixes or the `serpapi_supply_trends` retirement.
7. `docs/DATA_QUALITY_STRATEGY.md`: the improvement programme, validation against official statistics, the effective number of independent inputs, error bars, and the weekly ratchet that promotes or retires one source at a time. Adopted 2026-09-21. Paired with, and deliberately separate from, the audit below.
8. `docs/WEEKLY_PIPELINE_AUDIT.md` with `.github/workflows/weekly-pipeline-audit.yml` and `scripts/pipeline-audit.mjs`: a versioned runbook with a thin scheduled invoker. The docs steward follows the same pattern: the procedure lives in control-center and `.github/workflows/docs-steward.yml` only calls it.
9. `docs/AGENT_BRIEFING.md`, `docs/AUTONOMOUS_GTM_PLAYBOOK.md`, `docs/SALES_PLAYBOOK.md`: what an agent may say and do, in that order.
10. `docs/AGENT_INTEGRATION.md` and `docs/MCP_TOOL.md`: REST and MCP contracts. `docs/DESIGN_SYSTEM.md`: the interface contract. `docs/FLEET_WIRING.md`: attribution wiring to the OS warehouse, pending on the OS side.
11. `docs/NORTH_STAR.md` and `docs/MONETIZATION_STRATEGY.md`: short views of the same strategy; both name `CORPORATE_STRATEGY.md` as canonical.

## Do not trust

- `public/product-truth.json` `production_snapshot` and the two readback notes in `docs/DATA_SOURCES_ROADMAP.md` section 1 as a current reading: they are dated 2026-08-11 and say so. The 2026-08-29 collector fix and the 2026-09-21 integrity, dedup and retirement work are not reflected in them. Fetch `/fwi-api/current` and query `data_source_health`.
- Superseded 2026-09-21: the claim that migrations `008` and `009` (`pulse-daily-ingest`, `pulse-daily-insights`, `pulse-daily-redeploy`) were not found active as of the 2026-08-11 readback. The 2026-09-21 schedule readback in `docs/TECHNICAL_SPEC.md` section 2 found all of them active alongside `pulse-prepare-dataforseo-jobs` and the new `pulse-backstop-redeploy`; the commit message for `85a04d5` states the 11 August note was wrong when written or overtaken since, and that gap is how a duplicate daily collection ran unnoticed for weeks.
- `docs/TECHNICAL_SPEC.md` section 11 "Recent Major Changes": a partial list kept as history; `git log` is the record.
- `meta.nextUpdate` in `/fwi-api/current`: a legacy next-Sunday marker, not the next ingest time (`docs/AGENT_BRIEFING.md`).
- The 2026-08-07 note that the alert sending domain was unverified (`docs/DATA_SOURCES_ROADMAP.md` section 3): superseded 2026-09-21, the domain's DNS is verified. The pipeline now records which address actually delivered each alert rather than asserting the primary path works, so treat that record, not the comment, as current.
- The "21 tracked inputs" / "21 sources" figures anywhere in the tree describe the state before the 2026-09-21 `serpapi_supply_trends` retirement and are superseded by "20" wherever the retirement's claim-surface pass reached. The retirement **is** deployed: confirmed against production on 2026-09-24 (migration `019` applied, no health row, no signal row since 2026-09-18). Any remaining "not yet deployed" or "21 healthy sources" wording about this retirement, in this file's history or in commit messages from 2026-09-21, is superseded.
- The `as_of` date in this file's front matter as evidence that everything below it was re-read. The 2026-09-24 pass read production directly for source health, pipeline runs, signals, the latest score and applied migrations, and re-read `ingest-signals`. It did not re-verify the commercial, fleet-wiring or design sections, which still rest on their own earlier dates.
- Nothing else is superseded. `docs/NORTH_STAR.md`, `docs/CORPORATE_STRATEGY.md` and `docs/MONETIZATION_STRATEGY.md` overlap on prices and release gates and were checked for contradiction on 2026-09-07: none found. Prices, gates and the 31 October stop rule agree, and the pricing page shows the two tiers `MONETIZATION_STRATEGY.md` says it shows.
